def _sigma(data, start, end):
    sum = 0
    for i in range(start, end):
        sum = sum + data[i]
    return sum

def _maf(data, parameter):
    filtered = []
    for i in range(len(data)):

    return filtered

def _lpf(data, parameter):
    filtered = []
    
    return filtered